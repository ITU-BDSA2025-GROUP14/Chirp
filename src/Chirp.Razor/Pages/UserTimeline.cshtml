@page "/{author}"
@model Chirp.Razor.Pages.UserTimelineModel
@{
    ViewData["Title"] = "Chirp!";
    Layout = "Shared/_Layout";
}

<div>
    <h2> Private Timeline </h2>

    @if (Model.Cheeps.Any())
    {
        <ul id="messagelist" class="cheeps">
            @foreach (var cheep in Model.Cheeps)
            {
                <li>
                    <p>
                        <strong>
                            <a href="/@cheep.Author">@cheep.Author</a>
                        </strong>
                        @cheep.Message
                        <small>&mdash; @cheep.Timestamp</small>
                    </p>
                </li>
            }
        </ul>
    }
    else
    {
        <em>There are no cheeps so far.</em>
    }
</div>

<div>
    <ul class="paginationList">
        @if (Model.ShowPrevious)
        {
            <li class="page-item">
                <a href="/@Model.Author?CurrentPage=@(Model.CurrentPage - 1)">Previous</a>
            </li>
        }
        @if (Model.ShowNext)
        {
            <li class="page-item">
                <a href="/@Model.Author?CurrentPage=@(Model.CurrentPage + 1)">Next</a>
            </li>
        }
    </ul>
    <div>
        <ul class="pagination">
            @for (var i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a href="/@Model.Author?CurrentPage=@i">@i</a>
                </li>
            }
        </ul>
    </div>
</div>
